# Elktron Dashboard — Python dependencies
# Install: pip install -r requirements.txt

fastapi>=0.109.0
uvicorn[standard]>=0.27.0
websockets>=12.0
pyserial>=3.5
httpx>=0.27.0
pydantic>=2.5.0

# Hardware interfaces (install on Pi / dev machine only)
# lerobot               # Uncomment when arm hardware is connected
# picamera2             # Pi 5 only — escort bot camera
# gpiozero              # Pi 5 only — motor control
# lgpio                 # Pi 5 only — GPIO backend
